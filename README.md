🕹️ Онлайн-Магазин ПК-Игр

Полнофункциональное учебное веб-приложение с клиентской частью на Angular 18+, серверной частью на PHP 8+, поддержкой MySQL и возможностью работы через локальные JSON-моки.

Проект реализован по принципам MVP, SOLID-подходам и использует реактивную архитектуру на RxJS.

🚀 Основные возможности

Регистрация, вход, быстрый вход.

Роли пользователей: user / premium / moderator.

Просмотр каталога игр.

Продвинутая фильтрация (цена, рейтинг, жанры).

Добавление / редактирование / удаление игр.

Корзина и оформление покупки.

Оплата (симуляция + реальное списание с баланса).

Промокоды: CRUD, активация, деактивация, истории.

Полный список покупок, история транзакций.

Сохранение данных: MySQL и/или локальные JSON.

📦 Технологический стек
Client

Angular 18+

TypeScript

RxJS

HTML / SCSS

MVP + собственные шаблонные паттерны

LocalStorage (кеш, аватар, настройки, быстрый вход)

Server

PHP 8+

Мини-фреймворк на чистом PHP

PDO + MySQL

CORS-конфиг

Мягкое падение в JSON-моки при потере БД

📁 Структура проекта
Frontend (Angular)
src/
 ├── app/
 │    ├── components/      # UI-компоненты
 │    ├── services/        # Вся бизнес-логика
 │    ├── models/          # Интерфейсы данных
 │    ├── guards/          # AuthGuard
 │    ├── pages/           # Главная, корзина, профиль
 │    ├── utils/           # Unsubscriber helper
 │    └── app.routes.ts
 ├── assets/
 └── environments/

Backend (PHP)
api/
 ├── game.php
 ├── cart.php
 ├── user.php
 ├── payment.php
 └── promocodes.php
config/
 └── database.php
utils/
 ├── helpers.php
 └── corsoptions.php
MockData/
 ├── LOCAL-games.json
 ├── LOCAL-users.json
 ├── LOCAL-cart.json
 └── LOCAL-purchases.json

⚙️ Установка и запуск
1. Установить зависимости Angular
npm install

2. Запустить клиент
ng serve


Фронтенд доступен по адресу:
http://localhost:4200

🛠️ Настройка PHP-сервера
1. Размести серверную часть

Папки api/, config/, utils/, MockData/ — должны находиться в корне хостинга (обычно htdocs/, www/).

Пример:

C:/xampp/htdocs/pc-store/

2. Настрой CORS

Файл config/corsoptions.php уже настроен на:

http://localhost:4200


Если клиент работает на другом адресе — измени директиву:

header("Access-Control-Allow-Origin: http://localhost:4200");

3. Настрой MySQL

Создать базу:

pc_store_data


Импортировать структуру таблиц (если есть SQL-файл).
Если MySQL недоступна — PHP автоматически переключится на мок-данные JSON.

🌐 API — краткое руководство
📄 Получить список игр

GET

/api/game.php?action=getGames

➕ Добавить игру

POST

/api/game.php?action=addGame
{
  "data": {
    "title": "Example",
    "genre": "Action",
    "price": 100
  }
}

👤 Вход
POST /api/user.php?action=login
{
  "username": "test",
  "password": "123456"
}

🛒 Получить корзину
POST /api/cart.php?action=getCart
{ "user_id": 1 }

💳 Оплатить
POST /api/payment.php?action=pay
{ "user_id": 1 }

🎟️ Проверить промокод
POST /api/promocodes.php?action=checkPromo
{ "code": "DISCOUNT10" }

📙 Модели данных (кратко)

Важные интерфейсы Angular:

Game
interface Game {
  id: number;
  title: string;
  genre: string;
  rating: number;
  price: number;
}

User
interface User {
  id: number;
  username: string;
  password: string;
  balance: number;
  role: UserRole;
}

CartItem
interface CartItem {
  id: number;
  user_id: number;
  game_id: number;
  title: string;
  price: number;
}

Promocode
interface Promocode {
  code: string;
  discount: number;
  is_active: boolean;
}

🧩 Ключевые особенности проекта

Поддержка локальных JSON-моков (аварийный режим).

Сервисная архитектура (CartService, PaymentService, PurchaseService, PromocodeService…).

Реактивность через BehaviorSubject.

Раздельная логика UI и бизнес-функций.

Гибкая фильтрация и поиск.

Валидация карт и платежей.

Ролевая система доступа.

Настройки темы, аватара и профиля.

Полностью оформленный рабочий backend.

✔️ Запуск в 3 шага (коротко)

npm install

ng serve

Открыть http://localhost:4200 и убедиться, что PHP-сервер запущен.
